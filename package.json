{
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon server.js",
    "generate:usuarios": "npx prisma generate --schema=prisma/usuarios/schema.prisma",
    "generate:facturas": "npx prisma generate --schema=prisma/facturas/schema.prisma",
    "migrate:usuarios": "npx prisma migrate dev --schema=prisma/usuarios/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
